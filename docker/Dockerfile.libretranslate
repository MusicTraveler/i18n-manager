FROM libretranslate/libretranslate:latest

# The official image already has everything configured
# Models will be stored in /home/libretranslate/.local/share/argos-translate
# Mount a volume to this path on Railway for persistence

# Environment variables (can be overridden in Railway)
ENV LT_HOST=0.0.0.0
ENV LT_PORT=5000

# Expose port
EXPOSE 5000
